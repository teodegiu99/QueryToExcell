<Window
    x:Class="QueryToExcell.MainWindow"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">

    <Grid Padding="20">
        <StackPanel Spacing="10">
            <TextBlock Text="Sistema Estrazioni Dati" FontSize="24" FontWeight="Bold"/>

            <TextBlock Text="{x:Bind CurrentWindowsUser}" FontSize="14" Foreground="Gray"/>

            <TextBlock x:Name="TxtDebugInfo" FontSize="14" Foreground="Red" TextWrapping="Wrap" />

            <Button x:Name="BtnAddQuery" 
                    Content="➕ Aggiungi Nuova Query (Solo CED)" 
                    Visibility="Collapsed" />
        </StackPanel>
    </Grid>
    </Window>